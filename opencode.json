{
  "$schema": "https://opencode.ai/config.json",
  "agent": {
    "plan": {
      "description": "Read-only planning agent, run before every sprint",
      "mode": "plan"
    },
    "build": {
      "description": "Implements exactly one task at a time",
      "mode": "build"
    }
  }
}
