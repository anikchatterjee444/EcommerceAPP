info:
  name: Sort
  type: http
  seq: 7

http:
  method: GET
  url: http://localhost:3000/products?sort=price&order=asc
  params:
    - name: sort
      value: price
      type: query
    - name: order
      value: asc
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
