info:
  name: Price
  type: http
  seq: 6

http:
  method: GET
  url: http://localhost:3000/products?minPrice=100&maxPrice=500
  params:
    - name: minPrice
      value: "100"
      type: query
    - name: maxPrice
      value: "500"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
