info:
  name: Get phone
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:3000/products?search=phone
  params:
    - name: search
      value: phone
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
